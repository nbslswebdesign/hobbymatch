<div class="container">
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      
      <div class="text-center">
        <% if params[:plan] == '2' %>
          <h1>Pro Account</h1>
          <p>In just a few clicks you'll be a pro member for just £10 per month, giving you access to exclusive community events and more.</p>
        <% else %>
          <h1>Basic Account</h1>
          <p>Sign up for our free account and gain basic access to our community.</p>
        <% end %>
      </div>
      
      <div class="well">
        <h2>Sign up</h2>
          <% if params[:plan] == '2' %>
            <%= render "pro_form" %>
          <% else %>
            <%= render "basic_form" %>
          <% end <%>
        
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>